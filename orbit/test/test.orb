module Orb::Types::Intrinsics {
    type Unit
}

module Test with Orb::* {
    type constructor A<T>(a T)
    type constructor B<T>(b A<T>)
    type C

    alias D = B<C>

    (D) main () () {
        d = D(C())
    }
}