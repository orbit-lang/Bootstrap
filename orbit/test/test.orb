module Orb::Types::Intrinsics {
    type Unit
    type List<T>
    type Int

    type A(i Int, j Int)

    (self List) bar (a Int) (List) {
        return self
    }

    (self Unit) foo () (A) {
        return A(1, 1)
    }
}
