module A with Orb::Types::Intrinsics {
    trait T(x Int) {
        (self T) foo () (Int)
    }

    type A : T

    type projection A : T
        where x = 1

    (self A) test () (Int) {
        return self.foo()
    }
}
