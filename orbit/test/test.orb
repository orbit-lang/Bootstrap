module Orb::Types::Intrinsics {
    type Unit
    type Int
    type Symbol
    trait AnyType
}

module Orb::Core::Main {
    type Main
}

module Test with Orb::* {
    trait A(x Int, y Symbol) {
        (Self) new (n Int) (Self)
    }

    type B

    projection B : A
        where x by { 1 }
        where y = :a
        where new by (n) in { n }

    (Main) foo _ _ {
        b = B()
        c = B.new(1)
    }
}
