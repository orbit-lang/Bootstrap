module Orb::Types::Intrinsics {
    type Unit
    type Int

    type B(a A)
    type A(b B)
}

module Foo with Orb::Types::Intrinsics {
    type F(a A, b B)

    (self F) bar () () {
        a = A()
    }
}
