module Orb::Types::Intrinsics {
    type Unit
    type Int
    type Symbol
    trait AnyType
}

module Orb::Core::Main {
    type Main
}

module Test with Orb::* {
    trait constructor A<T>(t T)

    type B

    projection A : A<A>
        where t = 1

    (Main) foo _ _ {

    }
}
