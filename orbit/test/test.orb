@Main
module M with Orb::Types::Intrinsics with Orb::Core::Main {
    trait Show

    extension Show {
        (self Self) show () () {
            print self
        }
    }

    type projection AnyType : Show

    (self Main) main () () {
        a = AnyType()
        a.show()
    }
}