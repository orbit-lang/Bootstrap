module Orb::Types::Intrinsics {
    type Unit
    type Int
    type Symbol
}

module Orb::Core::Main {
    type Main
}

module Test with Orb::* {
    trait A(x Int, y Symbol) {
        (Self) new (n Int) (Self)
    }

    type B

    projection B : A
        where x by { 1 }
        where y = :a
        where new by { Self() }

    (Main) foo _ _ {
        b = B()
        c = B.new(2)
    }
}
