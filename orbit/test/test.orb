@Main
module M with Orb::Types::Intrinsics with Orb::Core::Main {
    trait Anything(x Int)

    type projection Int : Anything
        where x = 1

    type constructor Wrapped<N>(n N)
        where N : Anything

    type I = Wrapped<Int>
}