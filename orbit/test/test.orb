@Main
module M with Orb::Types::Intrinsics with Orb::Core::Main {
    (Int) foo () (Int) = 123

    (self Int) bar () () {
        print self
    }

    (self Main) main () () {
        Int.foo().bar()
    }
}