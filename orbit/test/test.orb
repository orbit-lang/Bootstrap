@Main
module M with Orb::Types::Intrinsics with Orb::Core::Main with Orb::Core::Types {
    trait Foo
    trait constructor Option<T>(value T)
        where T : Foo

    type Some : Option<Int>
    type IntSome = Some<Int>

    (self Main) main () () {
        o = IntSome(1)
    }
}
