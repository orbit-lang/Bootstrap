module Orb::Types::Intrinsics {
    type Unit
}

module Test with Orb::* {
    type String

    trait Writer(message String)

    projection String(self) : Writer
        where message = self

    (Unit) main () () {
        w = String()
        m = w.message
    }
}