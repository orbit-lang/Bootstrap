module Foo with Orb::Core::Main with Orb::Types::Intrinsics {
    trait Any
    type Foo : Any

    (self Any) foo () () {
        print self
    }

    (self Main) main () () {
        f = Foo()

        _ = f.foo()
    }
}
