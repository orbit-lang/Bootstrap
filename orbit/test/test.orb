module Orb::Types::Intrinsics {
    type Unit
    type Int
}

module Orb::Core::Main {
    type Main
}

module Test with Orb::* {
#    operator add by `+` (a Int, b Int) (Int)
#        by BootstrapOperator

    (self Main) main _ _ {
        i = 1 + 1
    }
}
