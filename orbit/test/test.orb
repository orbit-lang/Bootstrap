@Main
module M with Orb::Types::Intrinsics with Orb::Core::Types::PI with Orb::Core::Main {
    type constructor Vector<T>(elements Array<T>)

    (self Main) main () () {
        xs = [1, 2, 3]
        v = Vector<Int>(xs)
    }
}