module IntComparator with Orb::Types::Intrinsics {
    trait T(x Int)

    type projection Int : T
    type projection Symbol : T

    (Int) compare (a Int, b Int, c Symbol) (T) {
        return c
    }
}

