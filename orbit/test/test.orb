module Test {
    type T
    type A
    trait constructor Printable<T, U>(value Self[T], other Self[U])
    type Foo(value T, other A) : Printable<T, A>

    alias Bar = Printable<T, T>
    alias Baz = Bar

    type Quux(value T, other T) : Bar
}