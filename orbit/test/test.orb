module Test {
    type A
    type B
    trait T1(a A)
    trait T2(b B)
    type constructor Merge<A, B>

    alias T3 = synthesise(trait, A)

    trait Foo(a A)
    type Bar(a A) : T3

    type projection Bar : Foo
}