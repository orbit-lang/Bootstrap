@Main
module M with Orb::Types::Intrinsics with Orb::Core::Main {
    trait Show
    type Foo

    extension Show {
        (self Self) show () () {
            print self
        }
    }

    type projection Foo : Show

    (self Main) main () () {
        f = Foo()
        f.show()
    }
}