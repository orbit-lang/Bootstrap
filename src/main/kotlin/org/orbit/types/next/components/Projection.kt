package org.orbit.types.next.components

import org.orbit.types.next.inference.InferenceUtil

//data class Projection(override val baseType: TypeComponent, val trait: ITrait, val fields: List<Field>) : Extension<TypeComponent> {
//    override fun extend(inferenceUtil: InferenceUtil) {
//        inferenceUtil.addConformance(baseType, trait)
//    }
//}
