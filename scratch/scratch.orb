module People {
    type String
    type Int
    alias Person = { surname String, name String, age Int }
}

module M with People {
    trait Ordering {
        fun (self Self) compare (other Self) (Self)
    }

    projection Person : Ordering
        with compare by { x, y -> x }

    alias PersonOrdering = query T where T : Ordering
}